import { createMuiTheme } from "@material-ui/core";

const theme = createMuiTheme({
  palette: {
    primary: {
      main: "#333533",
    },
    secondary: {
      main: "#f4d58d",
      contrastText: "#a5a58d",
    },
  },
});

export default theme;
